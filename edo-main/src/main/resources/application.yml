server:
  port: 8761
spring:
  main:
    allow-bean-definition-overriding: true
  rabbitmq:
    username: "guest"
    password: "guest"
    host: "localhost"
    virtual-host: "/"
    port: "5672"
    template:
      routing-key: "common"
      default-receive-queue: "common"
      reply-timeout: 10000
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  dashboard:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: info
springdoc:
  swagger-ui:
    path: /swagger
